# Zeabur 环境变量配置模板
# 在 Zeabur Dashboard -> Service -> Variables 中配置

# ============================================
# 必填配置
# ============================================

# JWT 密钥（至少32字符的随机字符串）
# 生成方式: openssl rand -base64 32
JWT_SECRET=

# 数据加密密钥（必须正好32字符）
# 生成方式: openssl rand -base64 24 | head -c 32
ENCRYPTION_KEY=

# ============================================
# Redis 配置（Zeabur 自动注入）
# ============================================

# 添加 Redis 服务后，Zeabur 会自动注入以下变量：
# REDIS_HOST=${REDIS_HOST}
# REDIS_PORT=${REDIS_PORT}
# REDIS_PASSWORD=${REDIS_PASSWORD}
#
# 无需手动配置，Zeabur 会自动处理服务发现

# ============================================
# 可选配置
# ============================================

# 服务配置
NODE_ENV=production
PORT=3000

# 会话管理
STICKY_SESSION_TTL_HOURS=1
STICKY_SESSION_RENEWAL_THRESHOLD_MINUTES=15

# 功能开关
USER_MANAGEMENT_ENABLED=false
WEBHOOK_ENABLED=true
DEBUG_HTTP_TRAFFIC=false

# 监控配置
METRICS_WINDOW=5
LOG_LEVEL=info

# 时区配置（UTC偏移，中国为+8）
TIMEZONE_OFFSET=8

# ============================================
# 代理配置（香港节点通常不需要）
# ============================================

# 如果香港节点无法直连 Anthropic API，可在添加账户时配置代理
# 代理配置在 Web 管理界面中设置，不在环境变量中
